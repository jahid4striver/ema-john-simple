/**
 * 1. create a new project in console.google.com
 * 2. npm install firebase
 * 3. create firebase.initjs and import getAuth and export it
 * 4. firebase settings > Authentication > enable email and password Auth
 * 5. Create login, signup components and setup route
 * 6. Attach form field and submit handler
 * 7. npm install -save react-firebase-hooks
 * 8. useCreateUserWithEmailAndPassword(auth) from react-firebase-hooks
 * 9. if user is created redirect to the expected page
 * 10. use useSignInWithEmailAndPassword(auth) for sign In
 * 11. create requireAuth Components==> chekc user exists also track user locaktion
 * 12. In Route Wrap proteced components by sing requeire auth components
 * 
 * 
 * 
 */


/**
 * Hosting Steps
 * 1. npm install -g firebase-tools
 * 2. firebase login
 * 3. firebase init
 * 4. firebase deploy
 */